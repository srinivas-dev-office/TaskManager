// ignore_for_file: deprecated_member_use, constant_identifier_names, use_full_hex_values_for_flutter_colors




//routes

import 'package:taskmanager/core/routes/exports.dart';


double kTenFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 10.sp
        : 6.sp;
double kTwelveFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 12.sp
        : 7.sp;

double kFourteenFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 14.sp
        : 9.sp;
double kSixteenFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 16.sp
        : 11.sp;
double kEighteenFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 18.sp
        : 13.sp;
double kTwentyFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 20.sp
        : 15.sp;
double kTwentyTwoFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 22.sp
        : 17.sp;
double kTwentyFourFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 24.sp
        : 19.sp;
double kTwentySixFont =
    MediaQueryData.fromView(WidgetsBinding.instance.window).size.shortestSide <
            600
        ? 26.sp
        : 21.sp;

/// FONT WEIGHTS
const FontWeight kFW300 = FontWeight.w300;
const FontWeight kFW400 = FontWeight.w400;
const FontWeight kFW500 = FontWeight.w500;
const FontWeight kFW600 = FontWeight.w600;
const FontWeight kFW700 = FontWeight.w700;
const FontWeight kFW800 = FontWeight.w800;
const FontWeight kFW900 = FontWeight.w900;